<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <!-- Search -->
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_action_search_white_48dp"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="ifRoom|withText|collapseActionView"
        android:title="@string/action_search"/>

    <!-- Add host -->
    <item
        android:id="@+id/action_add_host"
        android:icon="@drawable/ic_add_circle_white_48dp"
        app:showAsAction="ifRoom|withText"
        android:title="@string/action_add_host"/>

    <!-- Reload -->
    <item
        android:id="@+id/action_reload_hosts"
        android:icon="@drawable/ic_refresh_white_48dp"
        app:showAsAction="never|withText"
        android:title="@string/action_reload"/>

    <!-- Select all -->
    <item
        android:id="@+id/action_select_all"
        android:icon="@drawable/ic_toggle_white_48dp"
        app:showAsAction="never|withText"
        android:title="@string/action_select_all"/>

    <!-- About -->
    <item
        android:id="@+id/action_about"
        android:icon="@drawable/ic_edit_white_48dp"
        app:showAsAction="never|withText"
        android:title="@string/action_about"/>
</menu>
